// Copy this file to Secrets.xcconfig (same directory) and replace the placeholder
// with your real Anthropic API key from https://console.anthropic.com/.
// Secrets.xcconfig is gitignored — never commit your real key.
ANTHROPIC_API_KEY = your-api-key-here
